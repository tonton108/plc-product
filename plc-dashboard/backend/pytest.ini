[pytest]
# Pytestの設定ファイル

# テストディレクトリ
testpaths = tests

# Pythonパスに現在のディレクトリを追加
pythonpath = .

# テスト実行時の表示オプション
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# マーカー定義
markers =
    slow: 時間のかかるテスト
    integration: 統合テスト
    unit: 単体テスト
